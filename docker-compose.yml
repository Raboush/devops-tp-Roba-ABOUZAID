version: "3.9"

services:
  backend:
    build: .
    container_name: backend-api
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      NODE_ENV: development
    restart: unless-stopped
